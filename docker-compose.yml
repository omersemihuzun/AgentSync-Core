version: "3.8"

services:
  api:
    build: .
    container_name: agentsync_api
    ports:
      - "8000:8000"
    volumes:
      - .:/app
    environment:
      - GEMINI_API_KEY=${GEMINI_API_KEY}
      - TWILIO_AUTH_TOKEN=${TWILIO_AUTH_TOKEN}
    restart: always

  # İleride eklenecek olan Frontend (Streamlit) servisi için taslak:
  # frontend:
  #   build: ./frontend_dir
  #   container_name: agentsync_frontend
  #   ports:
  #     - "8501:8501"
  #   depends_on:
  #     - api
